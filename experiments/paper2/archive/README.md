# Paper II archive

`commutant_pi_map.py` is retained for provenance only. Its restriction-map
codomain uses `End_G(V_lambda)` on nontrivial spectral layers that are not
`G`-invariant, so that map is not part of the revised Paper II foundation.
